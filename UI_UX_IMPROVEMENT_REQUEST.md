# 환전 어디서? UI·UX 및 입력 기능 개선 요청서

작성일: 2026-09-18\
대상: 현재 `exchange-web` 앱\
문서 상태: 개선 요구사항 정리. 구현 전.

## 1. 목표와 범위

처음 방문한 사용자가 가진 돈과 여행지를 선택하고, 환전소에 표시된 환율을 입력해 자신에게 맞는 환전 방법을 비교할 수 있도록 개선한다. 화면에는 판단과 입력에 필요한 내용을 우선 표시하고, 부가 설명은 정보 아이콘에 마우스 커서를 올렸을 때 확인하도록 한다.

이전 개선 제안의 번호를 그대로 사용한다.

| 구분 | 선택한 항목 | 요청 내용 |
|---|---|---|
| UI 디자인 | UI-1 | 모바일 결과 카드 배치 개선 |
| UI 디자인 | UI-2 | 입력 영역과 결과 영역 구분 |
| UI 디자인 | UI-3 | 금액 정보의 시각적 우선순위 정리 |
| UI 디자인 | UI-5 | 입력 라벨·버튼·키보드 포커스 개선 |
| UX | UX-1 | 사용자가 실제로 가진 돈에 맞춰 비교 경로 구성 |
| UX | UX-3 | 비교가 불완전한 상태에서 추천 표현 제한 |
| UX | UX-4 | 여행지 검색과 빠른 선택 제공 |
| 기능 | F-1 | 통화 변경 시 이전 환율이 계산에 남는 오류 수정 |
| 기능 | F-2 | 소수 금액 입력 오류 수정 |
| 공통 | COPY | 쉬운 문구로 수정하고 부가 설명을 정보 아이콘으로 이동 |

환율 그래프 확장, 단계별 계산 내역 기능, 별도 수수료 계산, 은행·서비스 추가, 환율 데이터 갱신 기능은 이번 요청 범위에 포함하지 않는다.

## 2. 문구와 정보 표시 원칙

### 2.1 환전소의 환율 표기는 유지한다

- `USD/THB`, `THB/USD`, `KRW/THB`, `THB/KRW`처럼 환전소 모니터와 대조하는 표기를 그대로 유지한다.
- `USD`, `EUR`, `THB` 등의 통화 코드와 `Buying`, `We Buy`, `Selling` 등 환전소에서 쓰는 표기를 임의로 바꾸거나 삭제하지 않는다.
- 두 방향의 환율 입력칸과 반대편 값 자동 표시 기능을 유지한다.
- 환율 표기 주변의 제목·캡션·설명만 쉬운 한국어로 다듬는다. 한국어 설명으로 기존 환율 표기를 대체하지 않는다.
- 기존 제안 중 환율 입력을 문장형 입력으로 교체하는 UX-2는 적용하지 않는다.

### 2.2 사용자가 해야 할 행동을 짧게 설명한다

- 제목은 “어떤 돈을 환전하나요?”, “어디로 여행하나요?”처럼 사용자가 바로 답할 수 있게 쓴다.
- 한 문장에는 한 가지 안내만 담는다.
- “역수”, “기회비용”, “벤치마크” 등의 설명용 전문용어는 기본 화면에서 줄인다.
- 숫자에는 통화와 비교 기준을 함께 표시한다.
- 예시 숫자를 실제 환율로 오해하지 않도록 `예시`임을 표시하고, 예시를 계산값으로 사용하지 않는다.
- 환율 오류, 입력 누락, 비교 대상 부족처럼 사용자가 다음 행동을 결정하는 데 필요한 안내는 숨기지 않는다.

### 2.3 필수 안내와 부가 설명을 구분한다

| 항상 표시할 내용 | 정보 아이콘에 숨길 내용 |
|---|---|
| 입력 항목 이름과 통화 단위 | 매매기준율·환율 우대·스프레드의 뜻 |
| `USD/THB` 등 환율 표기와 각 칸의 단위 관계 | 두 방향 환율의 관계와 계산 원리 |
| “환전소와 같은 표기의 칸에 숫자를 입력하세요.” | 반대편 환율이 자동으로 표시되는 원리 |
| 내는 돈에 해당하는 `Buying / We Buy` 확인 안내 | `Buying`과 `Selling`을 구분하는 추가 예시 |
| 입력 오류와 수정 방법 | 보유 달러·유로를 같은 가치로 비교하는 상세 설명 |
| 현재 비교 가능한 방법 수와 비교 범위 | 환율 출처와 계산 기준에 관한 상세 설명 |
| 수령액·통화·추천 조건 | 중복되는 설명, 용어 풀이, 보충 예시 |
| 카드/ATM 금액에 수수료를 반영하지 않았다는 점 | 카드/ATM 참고값의 계산 원리 |

### 2.4 정보 아이콘 동작

- 부가 설명이 있는 제목이나 용어 옆에 일관된 `ⓘ` 형태의 정보 아이콘을 배치한다.
- 설명은 기본 상태에서 숨기고, 사용자가 아이콘에 마우스 커서를 올렸을 때만 툴팁으로 표시한다.
- 커서를 아이콘에서 툴팁으로 옮겨도 설명을 읽을 수 있도록 유지하고, 두 영역을 벗어나면 닫는다.
- 클릭으로 고정하거나 본문을 펼치는 방식으로 변경하지 않는다.
- 툴팁 때문에 입력칸이나 결과 카드의 위치가 움직이지 않도록 한다. 화면 가장자리에서 설명이 잘리지 않게 배치한다.
- 툴팁 안에는 버튼, 링크, 입력칸처럼 추가 조작이 필요한 요소를 넣지 않는다.
- 아이콘에는 “환율 우대 설명”처럼 무엇을 설명하는지 알 수 있는 접근성 이름을 제공한다.
- 마우스를 올릴 수 없는 터치 환경에서도 비교를 완료할 수 있도록, 입력·판단에 필요한 정보는 본문에 남긴다. 이번 요청에서는 탭으로 여는 대체 동작을 추가하지 않는다.

## 3. UI 디자인 개선

### UI-1. 모바일 결과 카드 배치

**요청 사항**

- 모바일에서는 `환전 방법 → 예상 수령액 → 다른 방법과의 차이` 순서로 세로 배치한다.
- 수령액과 통화 단위를 붙여 읽을 수 있도록 구성한다.
- VND처럼 금액의 자릿수가 길어도 제목을 밀어내거나 카드 밖으로 넘치지 않게 한다.
- 설명을 작은 글자로 압축하기보다 줄바꿈과 세로 배치를 활용한다.

**완료 기준**

- 화면 너비 360px, 390px, 768px에서 결과 카드에 가로 스크롤이나 텍스트 겹침이 없다.
- 긴 금액을 표시해도 환전 방법, 수령액, 차액을 구분해 읽을 수 있다.

### UI-2. 입력 영역과 결과 영역 구분

**요청 사항**

- 데스크톱에서는 왼쪽에 입력, 오른쪽에 비교 결과를 배치한다.
- 모바일에서는 입력 다음에 결과를 배치하고, 결과가 준비되면 “비교 결과 보기” 버튼으로 이동할 수 있게 한다.
- 결과가 준비되지 않았다면 부족한 입력을 짧게 안내한다.
- 여행 팁은 핵심 입력과 결과 확인을 방해하지 않는 후속 영역에 배치한다.
- 고정 요소를 사용할 경우 결과·입력칸·기존 환율 정보 박스를 가리지 않게 한다.

**완료 기준**

- 데스크톱에서 입력을 변경하며 비교 결과를 확인할 수 있다.
- 모바일에서 결과 이동 버튼을 누르면 비교 결과 제목으로 이동한다. 키보드로 실행했을 때도 이동 위치를 알 수 있다.

### UI-3. 금액 정보의 시각적 우선순위

**요청 사항**

- 결과 카드에서 예상 수령액을 가장 강조하고, 다음으로 환전 방법과 차액을 구분해 보여준다.
- 환전 경로를 식별하던 `①②③④`와 실제 순위의 `1위·2위`가 함께 나타나 혼동되지 않도록, 사용자 화면에서는 경로 번호를 제거하고 방법 이름으로 구분한다.
- 추천 강조는 UX-3의 비교 조건을 충족할 때 적용한다.
- “손해”처럼 단정하는 표현을 줄이고, “최대 수령액보다 1.2% 적음”처럼 비교 기준을 명시한다.
- 원화 환산 차액을 표시할 경우 근사값임을 표시하고 계산 기준은 정보 아이콘으로 설명한다.

**완료 기준**

- 수령액, 방법 이름, 비교 차이를 글자 크기·간격·정렬로 구분할 수 있다.
- 색상 없이도 추천 여부와 비교 상태를 알 수 있다.

### UI-5. 입력 라벨·버튼·포커스

**요청 사항**

- 도시 검색, 통화 선택, 금액, 환율 입력칸에 의미가 분명한 라벨을 연결한다.
- 예시 문구만으로 입력칸을 구분하지 않는다.
- USD/EUR 선택, 다크모드, 환율 방향 변경 등 조작 요소에 충분한 클릭 영역과 간격을 확보한다. 주요 터치 조작은 44×44px 이상을 목표로 한다.
- 키보드로 이동할 때 현재 포커스가 보이게 하고, 선택된 통화와 펼침 상태를 보조 기술에도 전달한다.
- 다크모드에서도 라벨, 안내, 오류, 포커스를 구분할 수 있게 한다.

**완료 기준**

- 키보드만으로 필수 입력과 비교를 완료할 수 있다.
- 스크린리더가 입력칸의 이름과 통화 단위를 식별할 수 있다.
- 부가 설명 툴팁에 접근하지 않아도 필수 작업을 완료할 수 있다.

## 4. UX 개선

### UX-1. 가진 돈에 맞춘 비교 경로

**요청 사항**

- 사용자가 어떤 돈을 환전할지 먼저 선택하도록 한다. 원화와 이미 보유한 달러·유로를 구분한다.
- 원화만 가진 경우에는 원화로 시작하는 국내 환전, 현지 원화 환전, 달러·유로를 새로 바꾸는 이중 환전을 비교한다.
- 보유 외화 경로는 사용자가 해당 달러·유로를 가지고 있다고 선택한 경우에만 추천 대상에 포함한다.
- 예산을 표시하는 통화와 실제 보유 통화를 같은 의미로 취급하지 않는다. 보유 여부는 사용자의 명시적인 선택으로 확인한다.
- 보유 달러·유로와 원화 출발 경로를 함께 비교할 때에는 “같은 원화 가치의 금액으로 비교합니다.”라는 짧은 조건을 표시하고, 상세 기준은 정보 아이콘에 넣는다.
- 여러 지갑의 잔액을 합산하는 기능은 추가하지 않는다.

**완료 기준**

- 원화만 있다고 선택한 사용자에게 보유 달러·유로 환전을 최적 방법으로 추천하지 않는다.
- 보유 통화 선택을 바꾸면 해당 경로의 표시와 순위가 함께 갱신된다.

### UX-3. 비교 상태에 맞는 추천 표현

**요청 사항**

| 계산 가능한 현금 환전 방법 수 | 표시 방식 |
|---|---|
| 0개 | 필요한 입력을 안내하고 순위·추천을 표시하지 않는다. |
| 1개 | “현재 1개 방법만 계산할 수 있어요.”와 해당 금액을 표시한다. `1위`, `가장 유리` 표시는 사용하지 않는다. |
| 2개 이상 | “입력한 조건에서 가장 많이 받아요.”로 표시하고, 현재 비교한 방법 수를 함께 보여준다. |

- 계산에 필요한 환율이 없는 경로는 순위에서 제외하고 “환율을 입력하면 비교할 수 있어요.”라고 표시한다.
- 사용자가 이용할 수 없는 경로를 비교 개수와 순위에 포함하지 않는다.
- 카드/ATM 참고값은 현금 환전 순위와 비교 개수에 포함하지 않는다.
- 환율을 삭제하거나 통화를 바꾸면 기존 추천 표시도 즉시 갱신한다.
- 미입력 경로가 남아 있다면 전체 방법 중 최선으로 오해하지 않도록 “입력한 조건”이라는 비교 범위를 유지한다.

**완료 기준**

- 여행지만 선택한 상태에서 은행 환전을 `1위·가장 유리`로 표시하지 않는다.
- 비교 방법 수와 화면의 추천 상태가 일치한다.

### UX-4. 여행지 검색과 빠른 선택

**요청 사항**

- 도시명과 국가명으로 검색할 수 있게 한다.
- 검색 결과에는 도시와 국가를 함께 표시한다.
- 기존 도시 목록을 바탕으로 빠른 선택 항목을 제공한다. 실제 통계가 없다면 “인기 순위”라고 표현하지 않는다.
- 최근 선택한 여행지는 같은 브라우저에서 다시 선택할 수 있도록 저장한다.
- 도시를 고르면 해당 통화를 자동 선택하고, “방콕에서 쓸 돈: 태국 바트(THB)”처럼 확인할 수 있게 한다.
- 도시를 고르지 않고 통화만 직접 선택하는 기존 경로도 제공한다.
- 검색 결과가 없으면 “찾는 도시가 없나요? 통화를 직접 선택해 주세요.”라고 안내한다.

**완료 기준**

- “방콕”과 “태국”으로 해당 도시를 찾을 수 있다.
- 도시 선택과 현지 통화가 일치하며, 통화를 수동으로 바꾸면 맞지 않는 도시 선택을 정리한다.
- 검색 결과와 빠른 선택 항목을 키보드로 선택할 수 있다.

## 5. 입력 기능 오류 수정

### F-1. 통화 변경 시 이전 환율 초기화

**확인한 문제**

USD → THB 환율을 입력한 뒤 EUR로 바꾸면 입력칸은 비지만, 내부 계산에는 이전 USD 환율 숫자가 남는다.

**요청 사항**

- 실제 통화쌍이 바뀌면 해당 입력칸과 계산에 사용하는 환율 값을 함께 초기화한다.
- 새 환율을 입력하기 전까지 해당 환전 경로의 금액과 순위를 표시하지 않는다.
- 도시 변경으로 현지 통화가 바뀌거나 USD/EUR가 자동 전환되는 경우에도 같은 원칙을 적용한다.
- 통화쌍이 그대로인 다른 입력값은 불필요하게 지우지 않는다.
- 사용자가 읽는 값과 계산에 쓰는 값이 일치하도록 관리한다.

**완료 기준**

- USD → THB에 테스트 환율 `35`를 입력하고 EUR로 전환하면, EUR → THB 환율은 미입력 상태가 된다.
- 새 EUR 환율을 입력하기 전에는 이전 숫자 `35`로 계산한 수령액이나 순위가 나타나지 않는다.
- USD로 다시 돌아가도 삭제한 환율이 화면에 보이지 않는 상태로 계산에 재사용되지 않는다.

### F-2. 소수 금액 입력 보존

**확인한 문제**

금액 입력칸에 `100.5`를 순서대로 입력하면 소수점이 사라지고 `1,005`로 바뀐다.

**요청 사항**

- 입력 중에는 사용자가 작성한 문자열을 유지하고, 계산용 숫자와 분리한다.
- `100.`처럼 소수점을 입력하는 중간 상태를 허용한다.
- 쉼표 등 표시 형식은 입력을 마친 뒤 적용하거나, 입력 도중에도 소수점과 커서 위치를 보존하는 방식으로 처리한다.
- 달러·유로의 소수 금액을 입력할 수 있는 모바일 키보드를 제공한다.
- 빈 값, 0 이하, 여러 소수점, 숫자가 아닌 문자열은 유효한 예산으로 계산하지 않는다.
- 잘못 입력한 값은 입력칸 근처에 짧게 안내하고 임의의 다른 금액으로 바꾸지 않는다.

**완료 기준**

- `100.5`를 순서대로 입력하거나 붙여 넣으면 계산 금액은 모두 `100.5`다.
- `0.5`, `1,000.50`, 값 전체 삭제 후 재입력도 의도한 금액으로 처리한다.
- 문자열 중간 수정과 소수점 삭제·재입력 시 커서와 값이 예상과 다르게 바뀌지 않는다.

## 6. 문구 수정안

아래 문구를 기준으로 화면 전체의 제목, 캡션, 도움말, 오류 메시지를 정리한다. `{통화}`, `{도시}`, `{금액}`은 사용자가 선택하거나 계산한 값으로 바꾼다.

| 위치·기존 문구 | 수정안 | 표시 방식 |
|---|---|---|
| 앱 소개의 긴 환전 방식 나열 | “여행지에서 받을 돈을 비교해 보세요.” | 본문 |
| 예산 화폐 (내가 갖고 있는 돈) | “어떤 돈을 환전하나요?” | 본문. UX-1의 보유 여부 선택과 의미를 맞춤 |
| 여행지 선택 | “어디로 여행하나요?” | 본문 |
| 도시 (팁 제공) | “여행 도시” | 본문 |
| 직접 통화만 선택 | “통화로 직접 선택” | 본문 |
| 현지 화폐 | “여행지에서 쓸 돈” | 본문. 통화 코드는 유지 |
| 환전할 금액 | “얼마를 환전하나요?” | 본문. 통화 단위는 입력칸에 표시 |
| 현지 환전소 환율 입력 | “환전소에 표시된 환율” | 본문 |
| 아는 것만 입력해도 비교됩니다 | “알고 있는 환율만 입력하세요.” | 본문 |
| 둘 중 맞는 방향 칸에 입력…역수가 자동 표시… | “환전소와 같은 표기의 칸에 숫자를 입력하세요.” | 본문. 자동 표시 원리는 정보 아이콘 |
| Buying 설명의 긴 문단 | “내가 내는 돈의 Buying / We Buy 환율을 확인하세요.” | 본문. 추가 예시는 정보 아이콘 |
| USD/THB, THB/USD 등 | 기존 표기 유지 | 본문 |
| 1 THB = ? USD 등 단위 설명 | 기존 숫자·통화 관계 유지 | 각 입력칸 옆에 짧게 표시 |
| 하나은행에서 미리 환전 | “출국 전에 하나은행에서 바꾸기” | 결과 이름 |
| 현지 환전소 (원화) | “여행지에서 원화로 바꾸기” | 결과 이름 |
| 현지 환전소 (보유 외화) | “이미 가진 {달러/유로}로 바꾸기” | 결과 이름 |
| 이중 환전 | “{달러/유로}로 바꾼 뒤 한 번 더 환전하기” | 결과 이름. 용어 설명은 정보 아이콘 |
| 비교 결과 — 최종 수령 THB 기준 | “환전 방법별 예상 수령액” | 결과 제목. 선택한 통화는 함께 표시 |
| 가장 유리 ✓ | “입력한 조건에서 가장 많이 받아요.” | UX-3의 조건을 충족할 때만 표시 |
| 환전소 환율을 입력하세요 | “환율을 입력하면 비교할 수 있어요.” | 해당 결과에 표시 |
| 1위보다 −{차이율}% (≈{금액}원 손해) | “최대 수령액보다 {차이율}% 적음” | 본문. 원화 근사값은 보조 정보로 구분 |
| 100% 우대 카드/ATM (참고) | “카드·ATM 참고 금액” | 본문에 “수수료를 반영하지 않은 금액이에요.” 병기 |
| 고시환율 기준일 | “환율 기준일” | 본문 |
| 고시환율 로딩 중 | “환율을 불러오고 있어요.” | 상태 안내 |
| 고시환율과 크게 달라요 — 방향이 반대로… | “환율 방향을 확인해 주세요. 반대로 입력했을 수 있어요.” | 오류 안내. 숨기지 않음 |
| 방향 뒤집기 | “반대 방향으로 바꾸기” | 버튼 |

정보 아이콘의 설명도 짧게 쓴다. 예시는 다음과 같다.

- **환율 우대:** “은행이 환율에 붙이는 수수료를 얼마나 줄여 주는지 나타내요.”
- **반대편 환율 자동 표시:** “한쪽 칸에 입력하면, 같은 환율을 반대 방향으로 계산해 다른 칸에 표시해요.”
- **보유 외화 비교:** “이미 가진 달러·유로는 현재 기준 환율로 원화 가치를 계산해 비교해요. 예전에 산 가격으로 계산한 수익이나 손실은 아니에요.”

## 7. 구현 순서와 검증

### 권장 순서

1. F-1과 F-2의 입력 오류를 수정한다.
2. UX-1과 UX-3으로 비교 대상과 추천 조건을 정리한다.
3. UI-1·2·3·5와 UX-4를 적용한다.
4. 문구와 정보 아이콘을 화면 전체에 적용하고, 본문에 남겨야 할 안내를 다시 확인한다.

### 최종 확인 목록

- [ ] 선택한 UI 4개, UX 3개, 기능 2개와 공통 문구 개선을 반영했다.
- [ ] `USD/THB`, `THB/USD` 등 환전소 표기와 두 방향 입력 기능을 유지했다.
- [ ] 원화만 가진 사용자에게 보유 외화 경로를 추천하지 않는다.
- [ ] 하나의 현금 환전 방법만 계산되는 상태에서는 순위·최적 추천을 표시하지 않는다.
- [ ] 통화를 바꾸면 해당 환율 입력값과 계산값이 함께 초기화된다.
- [ ] `100.5`를 직접 입력하거나 붙여 넣어도 같은 금액으로 계산한다.
- [ ] 도시명·국가명 검색, 빠른 선택, 최근 선택, 통화 직접 선택이 동작한다.
- [ ] 긴 금액과 모바일 화면에서 글자가 겹치거나 가로로 넘치지 않는다.
- [ ] 정보 아이콘의 부가 설명은 기본적으로 숨기고 마우스를 올렸을 때 표시한다.
- [ ] 필수 안내와 오류는 툴팁을 열지 않아도 확인할 수 있다.
- [ ] 키보드 조작, 입력 라벨, 다크모드, 터치 환경을 점검했다.
- [ ] 기존 환전 계산 단위 테스트와 이번 입력 오류 재현 시나리오를 통과했다.

### 구현 시 참고할 파일

| 파일 | 주요 확인 영역 |
|---|---|
| `app/page.tsx` | 입력 상태, 보유 통화 선택, 화면 배치, 결과 순위, 문구 |
| `components/RatePairInput.tsx` | 기존 환율 표기 유지, 라벨, 설명, 방향 오류 안내 |
| `components/FloatingInfo.tsx` | 부가 설명 정리, 기존 환율 정보와 화면 배치의 간섭 |
| `app/globals.css` | 반응형 배치, 버튼·포커스·툴팁 스타일 |
| `data/cities.ts` | 도시·국가 검색과 빠른 선택에 사용할 기존 데이터 |
| `lib/calc.ts`, `lib/calc.test.ts` | 기존 계산 동작 보존 및 회귀 검증 |

실제 구현을 시작할 때는 `AGENTS.md`에 따라 `node_modules/next/dist/docs/`의 관련 가이드를 먼저 확인한다.
